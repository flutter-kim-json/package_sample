import 'package:flutter/material.dart';

/// 커스텀 텍스트 위젯입니다.
class CustomText extends StatelessWidget {
  const CustomText({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('커스텀 텍스트입니다');
  }
}
